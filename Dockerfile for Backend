In the root of your Node.js backend project, create a Dockerfile:

# Use Node.js 18 LTS
FROM node:18

# Set working directory
WORKDIR /usr/src/app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy the app
COPY . .

# Expose port
EXPOSE 5000

# Default command
CMD ["npm", "start"]
